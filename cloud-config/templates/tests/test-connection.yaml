apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloud-config.fullname" . }}-test-connection"
  labels:
{{ include "cloud-config.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "cloud-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
